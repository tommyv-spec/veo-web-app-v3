# Veo Web App Dependencies

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=21.0.0
python-multipart>=0.0.9
starlette>=0.38.0

# Database
sqlalchemy>=2.0.30

# Pydantic for data validation
pydantic>=2.9.0

# Authentication (Google OAuth)
authlib>=1.3.0
itsdangerous>=2.1.2

# Google Gemini / Veo 3.1
google-genai>=1.0.0

# OpenAI (for prompt tuning and auto-split)
openai>=1.10.0

# Image Processing (for celebrity filter bypass)
Pillow>=10.0.0

# HTTP client for API validation
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0
aiofiles>=23.0.0

# Voice Activity Detection (VAD) - OPTIONAL
# Uncomment these for silence removal in final video export
# torch>=2.0.0
# numpy>=1.24.0

# Audio Enhancement (lightweight, works on free tier)
numpy>=1.24.0
soundfile>=0.12.0
noisereduce>=3.0.0

# Voice Cloning uses httpx (already included above)
openpyxl>=3.1.0
psycopg2-binary>=2.9.9

# Flow backend support
redis>=5.0.0
boto3>=1.34.0
